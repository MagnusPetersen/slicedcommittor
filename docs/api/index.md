# API reference

The public surface is `sliced_committor.__all__` (the committor, the weights,
the directions, the feature-space metric, the rates) and
`sliced_committor.umbrella.__all__`. Everything else is private.

```{toctree}
:maxdepth: 1

committor
weights
directions
metric
rates
umbrella
```
